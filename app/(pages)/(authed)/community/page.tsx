import Auth from "@/components/wrapper/auth-wrapper";

export default function CommunityPage() {
    return (<Auth>
        <section className="flex flex-col gap-4 px-2">comm</section>
    </Auth>
    )
}